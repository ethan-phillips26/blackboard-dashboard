{
  "name": "whiteboard-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Desktop shell for the Whiteboard dashboard.",
  "scripts": {
    "sidecar": "uv run python scripts/build_sidecar.py",
    "app": "npm run sidecar && cargo build --release --manifest-path src-tauri/Cargo.toml",
    "bundle": "npm run sidecar && tauri build",
    "dev": "tauri dev",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
